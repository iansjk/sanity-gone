import { style } from "@vanilla-extract/css";

import { vars } from "../../../theme.css";

export const defenseIconPath = style({
  fill: vars.colors.accents.orange,
});

export const resIconPath = style({
  fill: vars.colors.accents.blue,
});
